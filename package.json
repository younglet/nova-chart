{
  "name": "nova-chart-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "terser src/nova-chart.js -c -m -o src/nova-chart.min.js && csso src/nova-chart.css -o src/nova-chart.min.css",
    "sync": "node scripts/sync-public.js",
    "predocs:dev": "npm run build && npm run sync",
    "predocs:build": "npm run build && npm run sync",
    "docs:dev": "vitepress dev docs --port 5176 --host 0.0.0.0",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "csso-cli": "^4.0.2",
    "terser": "^5.48.0",
    "vitepress": "^1.6.3"
  }
}
